import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    notSearchedContauner : {
        padding : 8,
        alignSelf : "center",
        marginTop : 150
    },
})

export default styles;